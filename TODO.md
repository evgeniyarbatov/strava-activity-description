# TODO

- ROADMAP.md's "Near-Term Next Steps" #1 (Afterglow/Tensions/Residue synthesis) isn't built — `scripts/describe.py` still just concatenates the nine lens sections; the sample journal entry has no synthesis.
- ROADMAP.md's #2 (`scripts/physiology.py` for HR/cadence/pace episodes) doesn't exist — the physiologist lens still infers from duration/weather only, same gap the roadmap names.
- No CI — the pytest suite under `tests/` (9 files) and pre-commit config only run locally.
